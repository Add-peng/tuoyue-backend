# 2026-04-22

## 后端推送任务
- 将 D:\tuoyue-backend-clean 项目重置 git 仓库，安全推送到 git@github.com:Add-peng/tuoyue-backend.git
- 包含 slowapi 限流 + Redis 任务持久化代码合并
- .gitignore 排除 venv/ 和 __pycache__/
- 远程仓库主分支 main，提交 SHA f13f0a1 → b685b35

## 短信验证码 Auth 系统（2026-04-24）
- 新增 sms_service.py：阿里云短信直调 OpenAPI（requests，无需 SDK），失败降级 console.log 打印
- 新增 user_store.py：Redis 用户存储 + JWT Token + PBKDF2 密码哈希（bcrypt 网络不通时的替代）
- 新增 main.py Auth 接口：send-code/verify-code/register/login/profile（含完整限频：IP/手机号/每日上限）
- 新增 .env 配置：ALIBABA_CLOUD_ACCESS_KEY_ID/SECRET, SMS_SIGN_NAME, SMS_TEMPLATE_CODE, JWT_SECRET
- .gitignore 已添加 .env 保护
- 注意：系统 pip 网络不通，无法安装 bcrypt/pyjwt(extra)/aliyunsdk-sdk；使用 pyjwt(已装) + hashlib PBKDF2 + requests 直调
- Redis 503 检查机制已加到所有 auth 端点

## 健康检查端点
- main.py 添加 @app.get("/health") 返回 {"status": "ok"}
- 位于根路由 "/" 之后，独立于其他接口，不冲突
- 已测试验证通过，提交 SHA 3051f96

## API 文档保护改造
- main.py 添加 ENVIRONMENT 环境变量控制 docs_url/redoc_url
- ENVIRONMENT=production 时 /docs 和 /redoc 返回 404
- 开发环境默认可用
- 已测试验证通过，提交 SHA b685b35

## 敏感词过滤中间件（2026-04-24）
- 新增 app/middleware.py：DFASensitiveFilter（DFA/Trie 算法），+ SensitiveWordMiddleware
- 新增 app/data/sensitive_words.json：下载自 Konsheng/Sensitive-lexicon（2408词），来源 GitHub TrChat 格式
- 中间件拦截 POST /api/generate，检测 topic 字段含敏感词则返回 HTTP 400
- 中间件已注册到 main.py（app.add_middleware）
- 提交 SHA f34c4e8

## 管理后台 API（2026-04-24）
- 新增 app/admin.py：5个管理接口，路径前缀 /api/admin
  - GET /api/admin/users（分页，Mock 100条）
  - GET /api/admin/users/{user_id}
  - PUT /api/admin/users/{user_id}/tier（支持 Free/VIP/Pro）
  - GET /api/admin/orders（分页，Mock 50条）
  - GET /api/admin/stats（平台统计）
- Pydantic 模型完整，Swagger 自动展示
- 预留 DB 扩展点（TODO 注释标注替换位置）
- admin.set_redis_client(redis_client) 已注入
- 提交 SHA f34c4e8

## 管理员重置密码接口（2026-04-24）
- 新增 POST /api/admin/users/{user_id}/reset-password
- 逻辑：生成8位随机字母数字密码 → PBKDF2哈希存Redis → 调用阿里云SMS发至用户手机
- Redis异常时优雅降级（捕获异常返回404，不崩服务）
- 新增 sms_service.send_password_sms()：密码通知短信专用函数（credentials未配置时降级console打印）
- user_store.reset_password()：Redis完整实现，含try/except包裹所有Redis操作
- 提交 SHA cd229b2
