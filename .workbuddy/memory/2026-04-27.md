# 2026-04-27 工作日志

## Prisma 初始化与 schema 定义（14:00-14:10）

- 安装 `prisma` Python 包（v0.15.0），阿里云镜像成功
- 创建 `schema.prisma`（项目根目录），定义三模型：
  - `User`：id(cuid), phone(unique), tier(Free), credits(100), createdAt, updatedAt
  - `Order`：id(cuid), userId, amount(varchar), credits, status(pending), tradeNo, paidAt, createdAt
  - `Agent`：id(cuid), name, description, agentId(unique), category, access, status, userCount, author, createdAt, updatedAt
- 踩坑：`prisma generate` 需要设置 `$env:PRISMA_GENERATOR_INVOCATION="1"` 才能正确触发生成，否则静默完成但不生成 `client.py`
  - 正确命令：`$env:PRISMA_GENERATOR_INVOCATION="1"; python -m prisma generate`
- 创建 `lib/__init__.py` + `lib/prisma.py`（全局 Prisma Client 单例，含 connect/disconnect/get_db）
- 修改 `main.py`：导入 lib.prisma，startup 连接 MySQL，shutdown 断开
- 修改 `Dockerfile`：加入 `prisma py fetch && prisma generate` 步骤
- 修改 `requirements.txt`：追加 `prisma>=0.15.0`
- `prisma db push` **待执行**：DATABASE_URL 中 `你的服务器公网IP` 为占位符，需用户填入真实 IP 后执行
- 提交 SHA：d4aed71（feat: init Prisma schema and push to MySQL）

## Prisma db push 与数据库授权（14:15-16:45）

### 关键发现
- 服务器公网 3306 端口对外关闭，本地无法直连
- 解决方式：建立 SSH 隧道 `ssh -L 3306:127.0.0.1:3306 root@8.218.14.40 -N -f`
- MySQL root 密码：`TuoyueABC1`（SSH 密码 ≠ MySQL 密码，需分别尝试）
- MySQL 用户：`pofengzhe`，密码 `ZzZdLe6Jp56n78my`，服务器 IP `8.218.14.40`

### 执行步骤
1. 建立 SSH 隧道 → 隧道连通 ✅
2. root 连接 MySQL → 密码 TuoyueABC1 正确 → 数据库 `tuoyue_db_new` 创建 ✅
3. 用 pymysql（本地已安装）通过隧道执行授权：
   - `CREATE USER 'pofengzhe'@'%' IDENTIFIED BY 'ZzZdLe6Jp56n78my'`
   - `GRANT ALL PRIVILEGES ON tuoyue_db_new.* TO 'pofengzhe'@'%'`
   - `FLUSH PRIVILEGES`
4. 切回 pofengzhe 用户执行 `prisma db push` → `The database is already in sync with your Prisma schema` ✅

### 数据库最终状态
- 用户 `pofengzhe` 有 `%`、`127.0.0.1`、`localhost` 三个 host 的访问权限
- `.env` 已配置：`mysql://pofengzhe:ZzZdLe6Jp56n78my@127.0.0.1:3306/tuoyue_db_new`
- 三张表已创建：users / orders / agents
- 提交 SHA：8c1b3fc（内存文件更新推送）

## 用户模块 MySQL 迁移（16:51-17:05）

### 新增文件
- `user_db.py`：Prisma MySQL 用户数据访问层，含：
  - `get_user_by_phone(phone)`：按手机号查询
  - `get_user_by_id(user_id)`：按 ID 查询
  - `create_user(phone)`：写入 User 表（credits=100 默认，phone 唯一冲突抛 ValueError）
  - `get_or_create_user(phone)`：注册即登录语义
  - `mysql_get_credits / mysql_grant_credits / mysql_deduct_credits`：积分 MySQL 操作

### 改造文件
- `billing_service.py`：
  - 移除了所有 Redis credits 操作
  - `get_credits / grant_credits / deduct_credits / has_sufficient_credits` 全部改为 MySQL
  - 同步入口用 `asyncio.run()` 封装，兼容 background_tasks 等 sync 上下文
  - Redis 保留（注入实例用于其他模块）
- `main.py`：
  - `/api/auth/register`：改用 `user_db.get_or_create_user()`，重复注册返回 409
  - `/api/auth/login`：改用 `user_db.get_user_by_phone()`，不再自动创建用户
  - `/api/user/profile`：从 MySQL 读取，响应字段含 credits
  - `UserProfileResponse`：移除 daily_quota，新增 credits

### 提交 SHA
- 8ba3a7d（feat: create Prisma client and migrate user module to MySQL）

## 订单与积分管理 MySQL 迁移（16:57-17:30）

### 新增文件
- `order_db.py`：Prisma MySQL 订单数据访问层，含：
  - `create_order(order_id, user_id, amount, credits)`：写入 Order 表（status=pending）
  - `get_order(order_id)`：按 order_id 查询
  - `mark_order_paid(order_id, trade_no)`：幂等标记订单已支付（status=paid + tradeNo + paidAt）
  - `is_order_paid(order_id)`：快速幂等检查

### 改造文件
- `payment_service.py`：
  - `create_order()`：MySQL Order 表写入（asyncio.run）+ Redis 缓存（TTL 2h）双写
  - `handle_paid_notify()`：MySQL 幂等检查 -> 更新 Order 表 -> 同步 Redis -> billing_service.grant_credits()
  - `get_order()`：优先查 MySQL，MySQL 不可用时降级查 Redis
- `billing_service.py`：`grant_credits/deduct_credits` 已是 MySQL，无需改造
- `app/admin.py`：`/grant-credits` 调用 `billing_service.grant_credits()`（-> MySQL），无需改造

### 数据层最终分层
- MySQL（Prisma）：User 表（用户信息+积分）、Order 表（订单权威来源）
- Redis：短信验证码、IP 频率限制、任务状态、订单缓存（TTL）
- billing_service.py：积分 CRUD 全走 MySQL

### 提交 SHA
- 4569efa（feat: migrate order and credit modules to MySQL）
