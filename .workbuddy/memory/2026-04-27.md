# 2026-04-27 工作日志

## Prisma 初始化与 schema 定义（14:00-14:10）

- 安装 `prisma` Python 包（v0.15.0），阿里云镜像成功
- 创建 `schema.prisma`（项目根目录），定义三模型：
  - `User`：id(cuid), phone(unique), tier(Free), credits(100), createdAt, updatedAt
  - `Order`：id(cuid), userId, amount(varchar), credits, status(pending), tradeNo, paidAt, createdAt
  - `Agent`：id(cuid), name, description, agentId(unique), category, access, status, userCount, author, createdAt, updatedAt
- 踩坑：`prisma generate` 需要设置 `$env:PRISMA_GENERATOR_INVOCATION="1"` 才能正确触发生成，否则静默完成但不生成 `client.py`
  - 正确命令：`$env:PRISMA_GENERATOR_INVOCATION="1"; python -m prisma generate`
- 创建 `lib/__init__.py` + `lib/prisma.py`（全局 Prisma Client 单例，含 connect/disconnect/get_db）
- 修改 `main.py`：导入 lib.prisma，startup 连接 MySQL，shutdown 断开
- 修改 `Dockerfile`：加入 `prisma py fetch && prisma generate` 步骤
- 修改 `requirements.txt`：追加 `prisma>=0.15.0`
- `prisma db push` **待执行**：DATABASE_URL 中 `你的服务器公网IP` 为占位符，需用户填入真实 IP 后执行
- 提交 SHA：d4aed71（feat: init Prisma schema and push to MySQL）
