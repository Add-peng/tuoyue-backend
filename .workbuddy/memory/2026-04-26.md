# 2026-04-26 工作记录

## 积分计费系统（2026-04-26）
- 新增 billing_service.py：积分计算、发放、扣减、查询功能
  - calculate_credits(prompt_tokens, completion_tokens)：ceil((tokens/1000)*1)，最低1积分
  - grant_credits(user_id, amount)：原子 INCRBY
  - deduct_credits(user_id, credits)：Lua 脚本原子扣减，不足时 (False, remaining)
  - has_sufficient_credits(user_id, min=10)：前置门槛检查
  - INITIAL_CREDITS=100，MIN_CREDITS_REQUIRED=10
- 修改 user_store.py：create_user 完成后自动 grant_credits(user_id, 100, "new_user_bonus")
- 修改 main.py：
  - 导入 billing_service，注入 billing_service.set_redis_client(redis_client)
  - run_crew_task 新增 user_id 参数；任务成功后模拟 token(200~800/100~500)，扣减积分
  - /api/generate 加前置积分检查：未登录跳过，积分<10 返回 HTTP 402
  - 新增 GET /api/user/credits：返回 {"credits": N}（需 JWT）
- 修改 app/admin.py：
  - 导入 billing_service
  - 新增 POST /api/admin/users/{user_id}/grant-credits：请求体 {"amount": N}，返回余额
- 提交 SHA 8cef01f
